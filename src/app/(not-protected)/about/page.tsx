import AboutPage from '@/components/AboutPage'
import React from 'react'

type Props = {}

export default function page({ }: Props) {
    return (
        <div>
            <AboutPage />
        </div>
    )
}